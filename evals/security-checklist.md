# Security Eval Checklist — Helios Solar

> Gate for Phase 6 deployment. All BLOCK items must pass.
> Run `/review` after each PR. Full review before every deploy.

## Headers (verify with https://securityheaders.com)
- [ ] `Strict-Transport-Security` present + preload ✅
- [ ] `X-Frame-Options: DENY` ✅
- [ ] `X-Content-Type-Options: nosniff` ✅
- [ ] `Referrer-Policy: strict-origin-when-cross-origin` ✅
- [ ] `Permissions-Policy` disables camera/mic/geo ✅
- [ ] `Cross-Origin-Opener-Policy: same-origin` ✅
- [ ] `Content-Security-Policy` present (per-request nonce) ✅
- [ ] No `Server: Next.js` header (`poweredByHeader: false`) ✅
- [ ] Grade A or A+ on securityheaders.com

## CSP (Content-Security-Policy)
- [ ] `script-src` has NO `'unsafe-inline'` ✅
- [ ] `script-src` uses `'nonce-{value}'` + `'strict-dynamic'` ✅
- [ ] `style-src` has `'unsafe-inline'` (required for Framer Motion) ✅
- [ ] `img-src` limited to `'self' data: blob:` ✅
- [ ] Nonce generated per-request (not static) ✅
- [ ] Dev-only `'unsafe-eval'` is gated behind `NODE_ENV !== 'production'` ✅
- [ ] All inline scripts carry the nonce prop
- [ ] CSP violation reports going nowhere sensitive (no `report-uri` to untrusted domain)

## Form security
- [ ] Rate limit: 5 req / 10 min per IP ✅
- [ ] CSRF: origin check against `NEXT_PUBLIC_SITE_URL` in production ✅
- [ ] Honeypot field silently drops bots ✅
- [ ] Time-trap: < 2500ms submissions dropped ✅
- [ ] Zod validates all fields server-side ✅
- [ ] CR/LF stripped from email fields ✅
- [ ] Turnstile CAPTCHA (env-toggled) ✅
- [ ] Error response for spam is 200 (not 400 — prevents bot enumeration) ✅

## Secrets & credentials
- [ ] No secrets in `src/` or `public/`
- [ ] `.env.local` in `.gitignore` ✅
- [ ] `.env.example` has no real values ✅ (only placeholders)
- [ ] `RESEND_API_KEY` only in environment, not code
- [ ] `TURNSTILE_SECRET_KEY` only in environment
- [ ] `NEXT_PUBLIC_*` vars contain no secrets

## Dependencies
- [ ] `npm audit` shows no high/critical vulns
- [ ] Dependencies are pinned (no `*` in package.json)
- [ ] `node_modules` in `.gitignore` ✅

## HTTPS / transport
- [ ] HSTS `max-age=63072000` (2 years) ✅
- [ ] HSTS `includeSubDomains; preload` ✅
- [ ] Production domain submitted to HSTS preload list
- [ ] No HTTP to HTTP redirects (all HTTPS)

## API routes
- [ ] All mutation routes (`POST`/`PUT`/`DELETE`) validate input with Zod
- [ ] Rate limiting on all public-facing routes
- [ ] No sensitive data in error responses (no stack traces in prod)
- [ ] API routes return proper HTTP status codes (200/400/429)

## Status: ⏳ Not yet run
Last run: N/A
Passed: 0/35
Blockers: 0
